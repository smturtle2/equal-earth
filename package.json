{
  "name": "equal-earth",
  "private": true,
  "version": "0.1.0",
  "license": "EUPL-1.2",
  "type": "module",
  "scripts": {
    "dev": "vite --config config/vite.config.ts --host 127.0.0.1",
    "build": "tsc --noEmit && vite build --config config/vite.config.ts",
    "preview": "vite preview --config config/vite.config.ts --host 127.0.0.1",
    "test": "vitest run --config config/vitest.config.ts",
    "test:browser": "playwright test --config config/playwright.config.ts"
  },
  "dependencies": {
    "gl-matrix": "^3.4.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@webgpu/types": "^0.1.72",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
